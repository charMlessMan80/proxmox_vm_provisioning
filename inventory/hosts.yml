---
# Provisioning is driven through the Proxmox API from the Ansible controller,
# so the play runs locally. The "proxmox" group exists mainly for grouping
# variables (see group_vars/).
all:
  children:
    proxmox:
      hosts:
        localhost:
          ansible_connection: local
